<p id="notice"><%= notice %></p>

<h1>Projects</h1>

<table class="table table-responsive">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Área</th>
      <th>Tipo</th>
      <th>Abstract</th>
      <th colspan="3"></th>
    </tr> 
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.field.name %></td>
        <td><%= project.expertise_area.area %></td>
        <td><%= project.abstract %></td>
        <td><%= link_to 'Evaluar', judge_evaluation_project_path(project.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>