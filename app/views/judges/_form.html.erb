<%= form_with(model: judge, local: true) do |form| %>
   <!-- <% if judge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(judge.errors.count, "error") %> prohibited this judge from being saved:</h2>
      <ul>
      <% judge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>-->

  <div class="field">
    <%= form.label :has_tablet %>
    <%= form.check_box :has_tablet %>
  </div>

  <div class="field">
    <%= form.label :department %>
    <%= form.number_field :department %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="btn btn-primary actions">
    <%= form.submit %>
  </div>
<% end %>
