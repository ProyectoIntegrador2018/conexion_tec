<% provide(:title, 'Crea tu proyecto') %>

<div class="container">

  <div class="row">
    <div class="col-12 col-md-6 offset-md-3">
      <h1>Actualiza tu proyecto</h1>
    </div>
  </div>
  
  <div class="row">
    <div class="col-12 col-md-6 offset-md-3">
      <%= form_for(@project, url: project_path) do |f| %>
        <%= f.fields_for :user, @project.user do |j| %>

        <%= render "errors_inverse" %>

        <div>
            <%= f.label :name, "Nombre del proyecto" %>
            <%= f.text_field :name, class: "form-control" %>
        </div>

        <div>
            <%= f.label :email, "Correo" %>
            <%= j.email_field :email, class: "form-control" %>
        </div>

        <div>
            <%= f.label :password, "Contraseña" %>
            <%= j.password_field :password, class: "form-control" %>
        </div>

        <div>
            <%= f.label :password_confirmation, "Confirmación" %>
            <%= j.password_field :password_confirmation, class: "form-control" %>
        </div>

        <% if session[:message] = "admin" %>
        <div>
          <%= f.label :field, "Field del proyecto"%>
          <%= f.text_field :field, class: "form-control" %>
        </div>

        <div>
          <%= f.label :client, "Cliente del proyecto" %>
          <%= f.text_field :client, class: "form-control"%>
        </div>

        <div>
          <%= f.label :abstract, "Abstract del proyecto"%>
          <%= f.text_field :abstract, class: "form-control" %>
        </div>

        <div>
          <%= f.label :video_url, "URL del video" %>
          <%= f.text_field :video_url, class: "form-control"%>
        </div>

        <div>
          <%= f.label :status, "Status del proyecto" %>
          <%= f.text_field :status, class:"form-control" %>
        </div>
        <% end %>

        <div class="mt-3">
            <%= f.submit "Editar proyecto", class: "btn btn-primary" %>
        </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

