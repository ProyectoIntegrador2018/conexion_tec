<tr>
	<td><%= link_to major.name, edit_common_major_path(major) %></td>
	<td>
		<%= link_to common_major_path(major), method: :delete, data: { confirm: '¿Estás seguro que deseas eliminar esta carrera?'} do %>
			<span style="color: red"><i class="fa fa-trash"></i> Eliminar</span>
		<% end %>
	</td>
</tr>