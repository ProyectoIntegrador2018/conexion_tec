<%= form_for @major, url: @url do |f|  %>
  <div class='row'>
    <div class='col-md-12'>
      <%= f.label :name, 'Carrera' %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <br>

  <div class='row'>
    <div class='col-md-6'>
      <%= link_to 'Cancelar', common_majors_path, class: 'btn btn-lightm btn-block' %>
    </div>
    <div class='col-md-6'>
      <%= f.submit 'Guardar', class: 'btn btn-primary btn-block', data: { confirm: '¿Estás seguro que deseas guardar los cambios?' } %>
    </div>
  </div>
<% end %>
