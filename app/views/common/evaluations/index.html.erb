<% provide(:title, "Evaluaciones") %>

<h1 style="text-align: center;">Evaluaciones</h1>

<% if @evaluations.length > 0 %>
    <table class="table table-responsive">
        <thead>
          <tr>
            <th>ID</th>
            <th>Result</th>
            <th>Proyecto</th>
            <th>Juez</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <tbody>
      <% @evaluations.each do |evaluation| %>   
      <tr>
          <td><%= evaluation.id %></td>
          <td><%= evaluation.result %></td>
          <td><%= evaluation.assignment.project.name %></td>
          <td><%= evaluation.assignment.judge.user.name %></td>
          <td><%= link_to 'Delete', common_evaluation_path(evaluation.id), method: :delete, data: {confirm: "Seguro que deseas borrar el Evaluacion #{evaluation.id}?"}  %></td>
      </tr>
      <% end %>
    </tbody>
    </table><br /><br />
<% else %>
    <div class="row">
        <div class="col-md-12">
          <div class="card border-dark mb-3">
            <div class="card-header">Aviso</div>
            <div class="card-body text-dark">
              <h5 class="card-title">No existen evaluaciones</h5>
              <p class="card-text">Por el momento no hay evaluaciones asignadas, para asignar un proyecto a un juez favor de ir a la sección de "Asignación de proyectos".</p>
            </div>
          </div>
        </div>
    </div>
<% end %>
