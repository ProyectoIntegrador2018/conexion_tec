<%= form_for @client, url: @url do |f|  %>
    <div class="row">
        <div class="col">
            <%= f.label :Cliente %>
            <%= f.text_field :client, class: "form-control" %>
        </div>
    </div><br />

    <div class="row">
        <div class="col">
            <%= link_to "Cancelar", admin_clients_path, { class: "btn btn-default" } %>
            <%= f.submit "Guardar", class: "btn btn-primary", data: { confirm: '¿Estás seguro que deseas guardar los cambios?' } %>
        </div>
    </div>
<% end %>