<%= form_for @expertise_area, url: @url do |f|  %>
<div class="row">
	<div class="col">
		<%= f.label :Área %>
		<%= f.text_field :area, class: "form-control" %>
	</div>
</div><br />

<div class="row">
	<div class="col">
		<%= link_to "Cancelar", admin_expertise_areas_path, { class: "btn btn-default" } %>
		<%= f.submit "Guardar", class: "btn btn-primary", data: { confirm: '¿Estás seguro que deseas guardar los cambios?' } %>
	</div>
</div>
<% end %>